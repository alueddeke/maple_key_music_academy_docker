# Development Environment Configuration for Maple Key Music Academy
# This is a TEMPLATE file - Copy this to env.dev and fill in your actual values
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp env.dev.example env.dev
# 2. Fill in your actual credentials in env.dev
# 3. Never commit env.dev to git (it's gitignored)

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database settings for development
POSTGRES_DB=maple_key_dev
POSTGRES_USER=maple_key_user
POSTGRES_PASSWORD=your_dev_password_here
POSTGRES_HOST=db
POSTGRES_PORT=5432

# =============================================================================
# DJANGO CONFIGURATION
# =============================================================================
# Django settings for development
SECRET_KEY=your-django-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,api,nginx,127.0.0.1,0.0.0.0

# Database URL for Django (alternative format)
DATABASE_URL=postgresql://maple_key_user:your_dev_password_here@db:5432/maple_key_dev

# =============================================================================
# CORS AND FRONTEND CONFIGURATION
# =============================================================================
# CORS allowed origins (comma-separated)
# Use '*' for development to allow all origins, or specify exact origins
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Frontend URL for OAuth redirects
FRONTEND_URL=http://localhost:5173

# =============================================================================
# DJANGO ALLAUTH CONFIGURATION
# =============================================================================
# Google OAuth settings (get these from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# =============================================================================
# EMAIL WHITELIST
# =============================================================================
# Comma-separated list of email addresses allowed to login
# Only these emails can authenticate (prevents spam/unauthorized access)
# Example: user1@gmail.com,user2@gmail.com,user3@gmail.com
ALLOWED_EMAILS=antonilueddeke@gmail.com,matt.james.morgan97@gmail.com,maplekeymusic.academy@gmail.com

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Vite/React environment variables
VITE_API_URL=http://localhost:8000/api
VITE_APP_NAME=Maple Key Music Academy
VITE_APP_ENV=development

# =============================================================================
# DEVELOPMENT FLAGS
# =============================================================================
# Flags to enable development features
USE_DOCKER=True
ENABLE_DEBUG_TOOLBAR=True
ENABLE_HOT_RELOADING=True

# =============================================================================
# EMAIL CONFIGURATION (Optional for local development)
# =============================================================================
# Gmail SMTP settings for sending PDF invoices
# For development, you can use your personal Gmail or create a test account
# Generate app password: https://myaccount.google.com/apppasswords
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password-here
DEFAULT_FROM_EMAIL=your-email@gmail.com
TEST_EMAIL_RECIPIENT=your-test-email@gmail.com
